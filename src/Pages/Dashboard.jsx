import Navbar from "../Components/Navbar"

export default function Dashboard(displayPage){
    return(
        <div id="dashboardContainer" className="Container flex flex-column">
            <Navbar />
        </div>
    );
}